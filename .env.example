ANTHROPIC_API_KEY=your_anthropic_api_key

# Provider: "kernel" (default) or "sandbox"
NEXT_PUBLIC_COMPUTER_USE_PROVIDER=kernel

# Kernel provider — required if NEXT_PUBLIC_COMPUTER_USE_PROVIDER=kernel
KERNEL_API_KEY=

# Vercel Sandbox provider — required if NEXT_PUBLIC_COMPUTER_USE_PROVIDER=sandbox
SANDBOX_SNAPSHOT_ID=your_snapshot_id
# Auth handled by `vercel link && vercel env pull` (VERCEL_OIDC_TOKEN) or:
# VERCEL_TOKEN=your_vercel_token
# VERCEL_TEAM_ID=your_vercel_team_id
# VERCEL_PROJECT_ID=your_vercel_project_id
